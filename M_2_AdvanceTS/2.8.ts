{
    //! Topic : Asynchhronous Typescript 

    //? Promise : 

    // simulate
    const createPromise = () => {
        return new Promise((resolve, reject) => {
            const data : string = "Something "
            if(data) {
                resolve()
            }
        } )
    }












    //
}